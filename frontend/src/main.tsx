import { StrictMode } from "react";
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root") as HTMLElement).render(
  <StrictMode>
    <main>
      <h1>Jellyfin Invite</h1>
      <p>Frontend scaffold ready.</p>
    </main>
  </StrictMode>
);
